<section class="md-section price-back" id="id-7">
  <div class="container">
    <div style="text-align: center">
      <h2>Hello there, you are in the automatic classic's viewer </h2>
      <h3>Type your class name to find your album</h3>
      <div class="row">
        <div class="col-lg-4 col-lg-offset-4">
          <div class="input-group">
            <%= form_tag(viewers_path, method: :post) do %>
              <input type="text" name="code">
              <%= submit_tag "Submit", class: 'btn btn-primary btn-submit' %>
            <% end %>
          </div>
        </div>
      <div>
    </div>
  </div>
</section>